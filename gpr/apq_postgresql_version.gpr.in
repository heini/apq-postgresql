---- author Daniel Norte de Moraes <danielcheagle@gmail.com>
---- tested in Debian Sid, Gcc 4.6 , gnat 4.4.6 , gprbuild 1.3.0-2 :-)
---- this project contem some information ( version and others )
---- this will be imported where make sense this informations :-)

limited with "apq-postgresql.gpr";

project apq_postgresql_version is

   version     := $version;     -- string
   name_base   := $name_base;   -- string
   --- type lib_installed_prefix_type is ( $prefix_list );  -- string_list  --- Does need of using o "rename" here ? :-) Obrigado Jesuis !!!!!!!!!!!! :-)

   lib_installed_prefix : apq_postgresql_version.lib_installed_prefix_type := apq_postgresql_version.lib_installed_prefix;
   system_libs  := $system_libs;

   include_path := "" & lib_installed_prefix & "/include/" & name_base & "/" & version ;

   lib_path     := "" & lib_installed_prefix & "/lib" ;
   gpr_path     := "" & lib_path & "/gnat";
   gpr_include_path :=  "" & include_path ;

end apq_postgsql_version;

